// Every basic move you want to do to a string is here!
// This is the header of its.

//#ifndef _stringRelated_
//#define _stringRelated_

extern void addLetterToString(char* inputStringSpace, int maxSize, char inputLetter);

extern void removeLastLetterInString(char* inputStringSpace, int maxSize);

extern char* inputPassword(char* inputStringSpace, int maxSize);

extern void deleteSpaceInTheEnd(char* inputStringSpace, int maxSize);
//#endif